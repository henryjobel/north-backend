import mongoose from "mongoose"; import { MONGO_URI } from "./src/config/siteEnv.js"; import { MenuItem } from "./src/models/menuItemModel.js"; mongoose.connect(MONGO_URI).then(async () => { await MenuItem.updateOne({ key: "static-dailyadin-newspaper" }, { $set: { label: "Daily Adin (NewsPaper)", href: "https://www.dailyadin.com/", external: true, source: "static" }, $setOnInsert: { key: "static-dailyadin-newspaper", sortOrder: 10, isVisible: true } }, { upsert: true }); console.log("Inserted"); process.exit(0); });
